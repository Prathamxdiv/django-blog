# TODO

- [x] Redesign blog hero section to a modern SaaS landing hero on `templates/blog/home.html`.
- [x] Add a Featured Articles section on the home page using existing `page_obj` data (template-only; no backend changes).
- [x] Add professional gradient background + glassmorphism-style visuals + hover/transition effects via `static/assets/css/main.css`.
- [x] Ensure responsiveness using Bootstrap grid + CSS media queries.
- [x] Validate templates render (via Django test client) and that server starts/checks pass.

